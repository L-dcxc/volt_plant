微型电压信号记录器设计方案

# 1.1 设备概况

针对多通道微弱电压采集、多地点分布式部署、离线存储与定期数据下载的需求，设计的一款低功耗微型电压信号记录仪。设备采用 STM32L431RCT6 主控搭配高精度 24 bit ADC，支持电池供电和本地 TF 卡存储，可满足在不同地点部署多件设备，后期通过上位机配置程序并通过 USB/串口下载数据文件的使用场景。

# 1.2 应用场景

森林中不同位置布置多件设备，每件设备接入以下多种传感器：1*温湿度0~2 V、1*光合有效辐射0~2 V、3*土壤各层含水0~2 V、6*树干水分**0~2.00 mV**。通过上位机设置记录器每1 min读取一次传感器数值，每10 min 记录一次期间测量值的均值，记录器将带有时间戳的数据保存于合适的文件类型中，如 .dat, .txt, .csv等。数据文件记录于板载 TF 卡中，通过 USB/串口连接上位机下载数据文件。

# 1.3 核心功能

**（1）0.01 mV精度多通道差分电压信号采集**：支持多通道24 bit电压信号采集，信号输入配置一定的滤波元件，以稳定测量 mV 级电压信号。满足自定义采样需求, 采样频次可在上位机自定义配置，如每30 s测量，每10 min 记录均值。

**（2）本地存储与 USB 下载：**内置 TF 卡本地数据存储，支持 USB 直连电脑，可直接读取数据文件，也可通过上位机进行波形查看与 CSV 导出。

**（3）实时时间戳记录：**内置 RTC 实时时钟（上位机同步），每条数据自动附带YYYY-MM-DD HH:MM:SS格式时间戳。

**（4）可配置外部控制输出：**预留 4 路独立 GPIO 控制引脚，支持自定义定时逻辑，即在上位机设置是否启动功能（如启动该功能，设置如间隔10分钟开启30秒的功能），驱动继电器开关模块控制传感器接线处闭合/开路，实现传感器间歇性加热控制（继电器、传感器等不由设备供电，我自行处理传感器供电线路）。

（5）上位机功能界面简洁设置即可

连接串口号

波特率

时钟（设置手动设定、设为系统时间的功能即可）

正在运行的程序（设置上传程序、启动程序、暂停运行、取回程序等即可）

通道面板（不需实时显示每通道数值，设置选中、读取所选通道当前信号数值的功能即可）

储存（设置通过串口下载文件功能即可）

# 1.4 已知可用硬件与预计规格

**（1）选用硬件**

模块

选型芯片

核心参数

主控

STM32L431RCT6

低功耗 STM32L4 系列 MCU，负责 ADC 采集、TF 卡存储、RTC 时间戳、串口/USB 上位机通信和 GPIO 控制

ADC

ADS124S08（TI）

AD7124-8 

24 bit Sigma-Delta ADC

支持多差分通道 

储存

板载Micro SD卡插口

支持TF卡扩充容量，可存储数年的采集数据，掉电数据不丢失

openlog 储存模块

集成式储存模块

供电

设计供电接线口

外接5-12 V供电电池

**（2）规格：**尺寸小于等于150 mm × 100 mm × 80 mm，工作湿度5% ~ 95% RH，工作温度 0 ~ 50 ℃。采样接口引出2 P GND + input, 或 公共端GND + input， 采样电压 0 ~ 5 V （> 0 ~ 2 V）避免传感器损坏时的高电压损坏设备。

**（3）其他选择性设计功能**

可选择性做外壳，3D打印等

设计指示灯与按钮，如正常工作时按下开关，指示灯亮绿表示设备有程序运行，指示灯红无程序运行

所有定时参数（采样间隔、记录间隔、上传间隔、控制引脚的通断	时长）可通过上位机软件灵活配置，适配不同的测试需求。