# Modbus RTU 寄存器映射

设备：STM32L431RCT6 植物记录仪  
接口：USART1 PA9(TX)/PA10(RX)，115200 8N1，TTL 直连，无 RS485 方向控制  
调试串口：USART2（printf 移到这里）  

支持功能码：FC03（读保持寄存器）、FC04（读输入寄存器）、FC06（写单个寄存器）、FC16（写多个寄存器）

---

## 一、保持寄存器（Holding Registers，FC03/FC06/FC16）

### 0x0000–0x000F 设备信息（只读）

| 地址   | 名称          | 类型   | 描述                      |
|--------|---------------|--------|---------------------------|
| 0x0000 | DEVICE_ID_H   | uint16 | device_id 高 16 位        |
| 0x0001 | DEVICE_ID_L   | uint16 | device_id 低 16 位        |
| 0x0002 | FW_VERSION    | uint16 | 固件版本，如 0x0100       |
| 0x0003 | CFG_VERSION   | uint16 | EEPROM 配置结构版本       |

### 0x0010–0x0019 系统配置（读写）

| 地址   | 名称               | 类型   | 描述                        |
|--------|--------------------|--------|-----------------------------|
| 0x0010 | MODBUS_ADDR        | uint16 | Modbus 地址 1–247           |
| 0x0011 | BAUD_H             | uint16 | uart_baudrate 高 16 位      |
| 0x0012 | BAUD_L             | uint16 | uart_baudrate 低 16 位      |
| 0x0013 | RUN_ENABLE         | uint16 | 0=停止采集，1=运行          |
| 0x0014 | AVG_ENABLE         | uint16 | 0=不平均，1=平均            |
| 0x0015 | FILE_FORMAT        | uint16 | 0=CSV，1=DAT，2=TXT         |
| 0x0016 | SAMPLE_INTERVAL_H  | uint16 | sample_interval_sec 高 16 位 |
| 0x0017 | SAMPLE_INTERVAL_L  | uint16 | sample_interval_sec 低 16 位 |
| 0x0018 | RECORD_INTERVAL_H  | uint16 | record_interval_sec 高 16 位 |
| 0x0019 | RECORD_INTERVAL_L  | uint16 | record_interval_sec 低 16 位 |

### 0x0020–0x0022 ADC 配置（读写）

| 地址   | 名称              | 类型   | 描述                  |
|--------|-------------------|--------|-----------------------|
| 0x0020 | ADC_VREF_MV       | uint16 | 参考电压 mV，默认 2048 |
| 0x0021 | ADC_DEFAULT_GAIN  | uint16 | 默认增益              |
| 0x0022 | ADC_DEFAULT_FILTER| uint16 | **保留 / 未启用**：固件固定使用 Sinc4 + 约 10 SPS + 50/60Hz 抑制；写入会保存到 EEPROM 但不影响硬件行为 |

### 0x0030–0x0033 RTC 时间（读写）

| 地址   | 名称      | 类型   | 描述                          |
|--------|-----------|--------|-------------------------------|
| 0x0030 | RTC_YEAR  | uint16 | 年，如 2026                   |
| 0x0031 | RTC_MONDAY| uint16 | [15:8]=月, [7:0]=日           |
| 0x0032 | RTC_HMIN  | uint16 | [15:8]=时, [7:0]=分           |
| 0x0033 | RTC_SEC   | uint16 | 秒                            |

写 RTC_SEC 后立即更新 RTC。

### 0x0040 控制命令（只写）

| 地址   | 写入值  | 动作                    |
|--------|---------|-------------------------|
| 0x0040 | 0x0001  | 保存当前配置到 EEPROM   |
| 0x0040 | 0x0002  | 恢复出厂默认并保存      |
| 0x0040 | 0x0003  | 软件复位                |

读 0x0040 恒返回 0x0000（无"上一条命令"概念）。写入未定义值返回异常 0x03。

### 0x0050–0x0053 控制输出手动强制（读写，**不保存到 EEPROM**）

运行期临时覆盖定时计划，掉电/复位后自动失效，恢复为定时模式。

控制路 n（0–3）地址 = `0x0050 + n`

| 写入值 | 含义                                   |
|--------|----------------------------------------|
| 0      | AUTO，跟随定时计划（默认）             |
| 1      | 强制 ON（忽略 interval/on_duration）   |
| 2      | 强制 OFF                               |

> 仅当该路 `FLAGS.enable=1` 时强制才生效；对未使能路写强制值返回异常 0x03。
> 强制生效时，输入寄存器 STATUS 的 bit2 置 1（见下）。

### 0x0060–0x0061 文件传输控制（读写）

文件传输采用"Modbus 协商 + YMODEM 搬运"混合方案：Modbus 负责列目录、选文件、读大小并触发，真正的文件字节流用 YMODEM 在同一串口上发送（详见第五节）。

| 地址   | 名称       | 类型   | 描述                                                       |
|--------|------------|--------|------------------------------------------------------------|
| 0x0060 | FILE_CMD   | uint16 | 写命令触发动作（见下表），读恒返回 0                       |
| 0x0061 | FILE_INDEX | uint16 | 目录中的文件序号（0 起），配合 SELECT/START/DELETE 使用    |

FILE_CMD 命令值：

| 写入值 | 动作                                                              |
|--------|-------------------------------------------------------------------|
| 0x0001 | OPEN_DIR：对 SD 卡数据目录拍快照，结果见 FILE_COUNT               |
| 0x0002 | SELECT：选中 FILE_INDEX 指向的文件，结果见 FILE_NAME/FILE_SIZE    |
| 0x0003 | START：开始用 YMODEM 发送当前选中文件（串口切入 YMODEM 模式）     |
| 0x0004 | DELETE：删除当前选中文件（可选，需谨慎）                          |

> 越界 FILE_INDEX 或未先 SELECT 就 START 返回异常 0x02/0x03。

### 0x0100–0x01FF 通道配置（16 通道 × 16 寄存器，读写）

通道 n（0–15）基地址 = `0x0100 + n × 0x10`

| 偏移  | 名称         | 类型   | 描述                                         |
|-------|--------------|--------|----------------------------------------------|
| +0x0  | FLAGS        | uint16 | [15:8]=enable（0/1），[7:0]=mode（0=单端，1=差分） |
| +0x1  | INPUTS       | uint16 | [15:8]=positive_input（AIN0–15=0–15），[7:0]=negative_input（AVSS=17） |
| +0x2  | SENSOR_GAIN  | uint16 | [15:8]=sensor_type，[7:0]=gain               |
| +0x3  | FILTER       | uint16 | **保留 / 未启用**：[15:8]=filter_mode 位定义保留以便后续扩展，当前固件忽略该字段；所有通道固定为 Sinc4 + 约 10 SPS + 50/60Hz 抑制 |
| +0x4  | RANGE_H      | uint16 | range_uv 高 16 位                            |
| +0x5  | RANGE_L      | uint16 | range_uv 低 16 位                            |
| +0x6  | OFFSET_H     | int16  | calib_offset_uv 高 16 位（有符号）           |
| +0x7  | OFFSET_L     | uint16 | calib_offset_uv 低 16 位                     |
| +0x8  | SCALE_H      | int16  | calib_scale_ppm 高 16 位（有符号）           |
| +0x9  | SCALE_L      | uint16 | calib_scale_ppm 低 16 位                     |
| +0xA  | WARMUP_H     | uint16 | warmup_ms 高 16 位                           |
| +0xB  | WARMUP_L     | uint16 | warmup_ms 低 16 位                           |
| +0xC–F| 保留         | —      | —                                            |

#### 校准 / 量程语义

- **RANGE_uv**：该通道满量程对应的输入电压（微伏），用于主机侧标度，不参与固件换算。常用值：2V 档 = 2000000，2mV 档 = 2000。
- **OFFSET_uv**（有符号）：零点偏移，单位 µV，从原始测量值中扣除。
- **SCALE_ppm**（有符号）：增益修正，以 ppm 表示，`1000000 = ×1.000000`（单位增益）。
- 主机得到的标定电压（µV）换算公式：

  ```
  Vcal_uV = (Vraw_uV - OFFSET_uv) × SCALE_ppm / 1000000
  ```

  其中 `Vraw_uV` 即输入寄存器 VIN_H/L 给出的值。固件已在 VIN 中完成此换算，主机直接使用 VIN 即可；OFFSET/SCALE 寄存器仅用于回读和重新标定。

### 0x0200–0x022F 控制输出配置（4 路 × 12 寄存器，读写）

控制路 n（0–3）基地址 = `0x0200 + n × 0x0C`

| 偏移 | 名称       | 描述                              |
|------|------------|-----------------------------------|
| +0   | FLAGS      | [15:8]=enable，[7:0]=output_id    |
| +1   | 保留       | —                                 |
| +2   | INTERVAL_H | interval_sec 高 16 位             |
| +3   | INTERVAL_L | interval_sec 低 16 位             |
| +4   | ON_DUR_H   | on_duration_sec 高 16 位          |
| +5   | ON_DUR_L   | on_duration_sec 低 16 位          |
| +6   | PHASE_H    | phase_offset_sec 高 16 位         |
| +7   | PHASE_L    | phase_offset_sec 低 16 位         |
| +8–B | 保留       | —                                 |

---

## 二、输入寄存器（Input Registers，FC04，只读，实时数据）

### 0x0000–0x003F ADC 通道测量值（16 通道 × 4 寄存器）

通道 n 基地址 = `n × 4`

| 偏移 | 名称    | 类型  | 描述                         |
|------|---------|-------|------------------------------|
| +0   | VIN_H   | int16 | 电压 uV，高 16 位（有符号）  |
| +1   | VIN_L   | uint16| 电压 uV，低 16 位            |
| +2   | RAW_H   | uint16| ADC 原始码高 16 位           |
| +3   | RAW_L   | uint16| ADC 原始码低 16 位           |

> 示例：读 CH0 对应地址 0x0000–0x0003，读 CH15 对应 0x003C–0x003F。

### 0x0040–0x0047 控制输出状态（4 路 × 2 寄存器）

控制路 n 基地址 = `0x0040 + n × 2`

| 偏移 | 名称      | 描述                                  |
|------|-----------|---------------------------------------|
| +0   | STATUS    | bit0=当前输出状态，bit1=使能标志，bit2=手动强制生效中 |
| +1   | REMAIN_S  | 当前状态剩余秒数（手动强制时为 0）    |

### 0x0048–0x004F 数据新鲜度

| 地址   | 名称           | 类型   | 描述                                          |
|--------|----------------|--------|-----------------------------------------------|
| 0x0048 | CH_VALID_MASK  | uint16 | bit n = 通道 n 当前 VIN 有效（已成功采样）    |
| 0x0049 | SAMPLE_COUNT   | uint16 | 每完成一轮采集 +1（溢出回绕），主机用于判新   |
| 0x004A | LAST_SMP_YEAR  | uint16 | 最近一次采样时间：年                          |
| 0x004B | LAST_SMP_MONDAY| uint16 | [15:8]=月，[7:0]=日                           |
| 0x004C | LAST_SMP_HMIN  | uint16 | [15:8]=时，[7:0]=分                           |
| 0x004D | LAST_SMP_SEC   | uint16 | 秒                                            |
| 0x004E–4F | 保留        | —      | —                                             |

### 0x0050–0x0053 系统状态

| 地址   | 名称       | 描述                                                            |
|--------|------------|-----------------------------------------------------------------|
| 0x0050 | SYS_STATUS | bit0=ad7124_ready，bit1=eeprom_ok，bit2=rtc_ok，bit3=sd_ok，bit4=running |
| 0x0051 | UPTIME_H   | 系统运行时间 ms 高 16 位                                        |
| 0x0052 | UPTIME_L   | 系统运行时间 ms 低 16 位                                        |
| 0x0053 | ERR_CODE   | 最近一次错误码（见下表，0=正常）                                |

### 0x0054–0x0057 SD 卡容量（只读）

| 地址   | 名称         | 类型   | 描述                          |
|--------|--------------|--------|-------------------------------|
| 0x0054 | SD_TOTAL_MB_H| uint16 | SD 卡总容量 MiB，高 16 位     |
| 0x0055 | SD_TOTAL_MB_L| uint16 | SD 卡总容量 MiB，低 16 位     |
| 0x0056 | SD_FREE_MB_H | uint16 | SD 卡剩余容量 MiB，高 16 位   |
| 0x0057 | SD_FREE_MB_L | uint16 | SD 卡剩余容量 MiB，低 16 位   |

> 容量由固件在每次写盘后缓存刷新（基于 FatFs `f_getfree`），SD 卡未就绪时全为 0。
> **写满策略：环形覆盖** —— 当剩余空间低于固件阈值（约 20 MiB）时，自动删除最早的数据文件（按 YYMMDD 文件名最小者），但绝不删除当天正在写入的文件。

### 0x0058–0x0059 电池/参考电压（只读）

| 地址   | 名称       | 类型   | 描述                              |
|--------|------------|--------|-----------------------------------|
| 0x0058 | BATTERY_MV | uint16 | 电池电压 VBAT，单位 mV            |
| 0x0059 | VDDA_MV    | uint16 | 实测 VDDA（ADC 参考），单位 mV    |

> 固件每 30 s 采样一次：先经内部 VREFINT 出厂校准反算实际 VDDA，再读 BAT_V_ADC（VBAT 经 R19/R23 两个 10kΩ 分压，节点 = VBAT/2），还原得 VBAT。
> 采样由 `BAT_ADC_EN`（PC1）门控：平时拉低关断分压支路省漏流，采样前拉高、稳定后再读、读完拉低。
> 未就绪/未采样时 BATTERY_MV 为 0。**单节锂电（TP4056 充电管理）**：满电 4.2 V、标称 3.7 V、截止 ~3.0 V；上位机对 <3.4 V 提示偏低、<3.3 V 告警。

#### ERR_CODE 错误码表

| 值     | 含义                          |
|--------|-------------------------------|
| 0x0000 | 正常                          |
| 0x0001 | AD7124 通信/采样故障          |
| 0x0002 | EEPROM 读写故障               |
| 0x0003 | RTC 故障                      |
| 0x0004 | SD 卡挂载/写入故障            |
| 0x0005 | 配置 CRC 校验失败，已回退默认 |
| 0x00FF | 未知错误                      |

### 0x0060–0x0074 文件传输状态（只读）

| 地址        | 名称           | 类型   | 描述                                                       |
|-------------|----------------|--------|------------------------------------------------------------|
| 0x0060      | FILE_XFER_STATE| uint16 | 0=空闲，1=目录就绪，2=已选中，3=传输中，4=完成，5=错误     |
| 0x0061      | FILE_COUNT     | uint16 | OPEN_DIR 后目录内文件数                                    |
| 0x0062      | FILE_SIZE_H    | uint16 | 选中文件大小（字节）高 16 位                               |
| 0x0063      | FILE_SIZE_L    | uint16 | 选中文件大小（字节）低 16 位                               |
| 0x0064–0x0073 | FILE_NAME    | char[] | 选中文件名，ASCII，每寄存器 2 字符（高字节先），不足补 0   |
| 0x0074      | FILE_ERR       | uint16 | 文件子系统错误码（0=正常，与 ERR_CODE 中 SD 类错误一致）   |

---

## 三、通信时序说明

- **帧间静默**：3.5 字符时间（115200 约 0.3 ms × 3.5 ≈ 1 ms，实现取 2 ms 保险）
- **响应超时**：主机等待 100 ms 无响应视为超时
- **写配置流程**：FC16 写寄存器 → FC06 写 0x0040=0x0001 触发 EEPROM 保存
- **改 Modbus 地址/波特率**：写完后需保存并复位生效
- **32 位值字节序**：高字寄存器地址低，低字寄存器地址高（big-endian 分割）；寄存器内字节序为 Modbus 标准大端（高字节先发）
- **广播地址 0**：从机执行写操作但**不应答**；FC03/FC04 读不支持广播
- **读取原子性**：实时数据（VIN/RAW/状态）由采集中断更新，构造 FC04 响应时固件须对涉及寄存器做一次性快照，避免主机读到半新半旧的 32 位值

### 异常响应（Exception Response）

从机检测到错误时，功能码最高位置 1（功能码 + 0x80），后跟 1 字节异常码：

| 异常码 | 名称                | 触发场景                                              |
|--------|---------------------|-------------------------------------------------------|
| 0x01   | 非法功能码          | 收到非 FC03/04/06/16                                  |
| 0x02   | 非法寄存器地址      | 访问未定义地址，或读写数量越界                        |
| 0x03   | 非法数据值          | 写值超范围（如 MODBUS_ADDR 不在 1–247、波特率非法、向只读区/未使能控制路写强制值） |
| 0x04   | 从机设备故障        | 内部处理失败（如 EEPROM 写超时）                      |

> 写入策略：非法值**拒绝整帧并返回 0x03**，不做钳位，语义更明确。FC16 多寄存器写为"全有效才执行"，其中任一非法则整帧拒绝、不部分写入。

---

## 五、文件下载（Modbus 协商 + YMODEM 传输）

USART1 平时跑 Modbus；下载文件时临时切入 YMODEM 发送模式，传完切回。两个应用层协议共用同一根串口，靠 START 命令显式切换。

### 下载流程

1. 主机 FC06 写 `FILE_CMD=0x0001`（OPEN_DIR）→ 设备拍目录快照
2. 主机 FC04 读 `FILE_COUNT` 得文件数
3. 逐个：FC06 写 `FILE_INDEX=i` + `FILE_CMD=0x0002`（SELECT）→ FC04 读 `FILE_NAME`/`FILE_SIZE`，构建文件列表给用户
4. 用户选定第 k 个：FC06 写 `FILE_INDEX=k` + `FILE_CMD=0x0003`（START）
5. 设备回完该 FC06 应答后，**串口切入 YMODEM 发送模式**，`FILE_XFER_STATE=3`
6. 主机作为 YMODEM 接收方发起（发 `C`），设备发送块 0（含文件名+大小）、数据块、EOT，标准 YMODEM 流程
7. 传输结束设备切回 Modbus，`FILE_XFER_STATE=4`（成功）或 `5`（失败，查 `FILE_ERR`）

### 模式切换与容错

- **握手天然安全**：START 的 FC06 应答发完后设备才进入 YMODEM；YMODEM 由接收方发 `C` 触发，设备等到 `C` 才开始发块 0。主机应在收到 START 应答后再启动 YMODEM 接收。
- **超时退回**：设备进入 YMODEM 后若 N 秒（建议 10 s）未收到 `C`，或传输中连续 NAK/超时，则放弃、回 Modbus、置 `FILE_XFER_STATE=5`。
- **中止**：收到 YMODEM CAN-CAN（两个 0x18）立即中止并回 Modbus。
- 传输期间设备不响应 Modbus（串口被 YMODEM 占用），主机不应在此期间发 Modbus 请求。
- 数据吞吐：115200 下 YMODEM 约 10 KB/s（Modbus 约 6~8 KB/s），大文件优势明显。

---

## 六、实现文件规划

| 文件 | 内容 |
|------|------|
| `mycode/modbus_rtu.h/.c` | CRC16、帧解析、FC03/FC04/FC06/FC16 响应构造、异常响应 |
| `mycode/app_modbus.h/.c` | 寄存器地址映射，绑定 app_config、实时数据、文件控制面 |
| `mycode/ymodem.h/.c` | YMODEM 发送（块 0 文件名/大小、1K 数据块、CRC16、EOT、超时/中止），从 SD 读文件 |
| `Core/Src/main.c` | printf 重定向改到 USART2，主循环调用 Modbus 处理；START 时调度 YMODEM 并在结束后恢复 Modbus |
