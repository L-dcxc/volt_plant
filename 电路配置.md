# 电路配置

本文档记录采集盒当前硬件电路与 STM32CubeMX 外设配置要点，方便后续软件开发时快速查阅。

## 整体复查结论

- **当前已配置的主要外设**：HSE、LSE 引脚、SWD、GPIO 电源控制、SDMMC1、FatFs、I2C2、SPI1、ADC1、USART1/2/3、IWDG、TIM6、TIM7。
- **本次发现的漏项**：`ADC_PWR_EN` 已在 CubeMX 中补为 `PA0 / GPIO_Output`，文档中已补充。
- **默认电平设计策略**：`PWR_ON`、`ADC_PWR_EN`、`SD_PWR_EN`、`BAT_ADC_EN` 在生成代码中默认都是低电平，这是当前硬件/低功耗设计策略。
- **建议到 CubeMX 中确认的项**：
  - `PWR_ON` 默认 Low 用于按键开关逻辑，软件进入运行后再按需要拉高保持电源。
  - `ADC_PWR_EN` 默认 Low 用于低功耗，测试或采集 AD7124 前在用户代码中拉高并延时。
  - `SD_PWR_EN` 默认 Low 用于低功耗，访问 SD 卡前必须拉高并延时。
  - `SD_CD` 当前只是 GPIO 输入，软件暂未真正使用插卡检测。
  - `ON_OFF`、`KEY_PWR` 当前 GPIO 无内部上下拉，需依赖外部电路；如果实测悬空，应在 CubeMX 中配置合适上下拉。
  - `LSE` 引脚已经占用为 32.768 kHz 晶振，但当前工程没有启用 RTC，FatFs 时间戳仍为 0。

## 主控

- **MCU**：`STM32L431RCT6`，LQFP64
- **主供电**：`+2.8V_MCU`
- **硬件 VDDA/VDD**：约 `2.8V`
- **系统时钟**：
  - HSE：8 MHz 外部晶振，当前系统时钟配置为 80 MHz
  - LSE：32.768 kHz 晶振引脚已配置，但当前软件未启用 RTC
- **调试接口**：SWD
  - `PA13`：`SWDIO`
  - `PA14`：`SWCLK`
- **复用注意**：
  - `PA15` 被用作 `SD_CD`，JTAG 的 JTDI 不再使用
  - `PB3` 被用作 `SD_PWR_EN`，SWO/TRACESWO 不再使用

## 电源控制

- **电源使能类信号**：高电平有效
- **`PWR_ON`**：`PB12`
  - 高电平有效
  - 用于系统电源保持/主电源控制，具体逻辑以电源管理电路为准
  - 如果是按键开机自锁电路，软件应尽早拉高
  - 当前 CubeMX 生成默认低电平，用于配合按键开关设计
- **`ADC_PWR_EN`**：`PA0`
  - 高电平有效
  - 用于 ADC 模块电源使能，关联 `+2.8V_ADC` 相关电源
  - 测试 AD7124 前必须拉高，并等待电源稳定
  - 当前 CubeMX 已配置为 GPIO 输出，生成默认低电平，用于低功耗按需开启
- **`SD_PWR_EN`**：`PB3`
  - 高电平有效
  - 控制 SD 卡电源芯片 `MSXC6210B282MR` 的 `EN` 脚
  - `SD_PWR_EN = 1`：打开 `+2.8V_SD`
  - `SD_PWR_EN = 0`：关闭 `+2.8V_SD`
  - 使用 SD 卡前应先拉高 `SD_PWR_EN`，并延时等待电源稳定后再挂载 FatFs
- **`BAT_ADC_EN`**：`PC1`
  - 高电平有效
  - 控制电池电压分压采样电路
  - 默认低电平可以降低静态功耗

## SD 卡

- **接口**：SDMMC1，4-bit bus
- **电源**：`+2.8V_SD`，由 `SD_PWR_EN` 控制
- **数据/命令线上拉**：已做外部上拉
- **CLK**：无上拉，符合常规 SDMMC 设计
- **引脚映射**：
  - `PC8`：`SDMMC1_D0` / `SD_D0`
  - `PC9`：`SDMMC1_D1` / `SD_D1`
  - `PC10`：`SDMMC1_D2` / `SD_D2`
  - `PC11`：`SDMMC1_D3` / `SD_D3`
  - `PC12`：`SDMMC1_CK` / `SD_CK`
  - `PD2`：`SDMMC1_CMD` / `SD_CMD`
  - `PA15`：`SD_CD`
- **CubeMX 配置要点**：
  - SDMMC1 4-bit Wide bus
  - Hardware Flow Control：Enable
  - SDMMC kernel clock：32 MHz
  - ClockDiv：2
  - DMA：RX/TX Normal 模式，Word 对齐，High priority
  - NVIC：SDMMC1、DMA2_Channel4、DMA2_Channel5 已启用
  - FatFs：SD Card 模式已启用
- **开发注意**：
  - `MX_SDMMC1_SD_Init()` 当前主要初始化 `hsd1` 参数，真正访问 SD 卡通常从 FatFs `f_mount()` / diskio 初始化触发。
  - 当前 FatFs 生成的是阻塞式 `HAL_SD_ReadBlocks()` / `HAL_SD_WriteBlocks()` 路径，不是 DMA template。
  - `BSP_SD_IsDetected()` 当前默认返回 `SD_PRESENT`，暂未真正使用 `SD_CD` 判断插卡状态。
  - `SD_CD` 当前为 GPIO Input / No Pull，若卡座检测脚没有外部上下拉，可能悬空，需按实测在 CubeMX 中配置 Pull-up 或 Pull-down。

## EEPROM

- **芯片**：`BL24C64A`，24C64 系列 EEPROM
- **接口**：I2C2
- **供电**：`+2.8V_MCU`
- **地址脚**：`A0/A1/A2` 接地
- **7-bit I2C 地址**：`0x50`
- **HAL 地址写法**：`0x50 << 1`，即 `0xA0`
- **引脚映射**：
  - `PB10`：`I2C2_SCL`
  - `PB11`：`I2C2_SDA`
- **硬件上拉**：`SCL/SDA` 通过 `3.6kΩ` 上拉到 `+2.8V_MCU`
- **CubeMX 配置要点**：
  - I2C Standard Mode，100 kHz
  - 7-bit addressing
  - Analog Filter：Enable
  - Digital Filter：0
  - GPIO：AF Open-Drain，No Pull，依赖外部上拉
- **开发注意**：
  - 24C64 内部地址使用 16-bit memory address。
  - 写入需要按页处理，常见页大小为 32 bytes。
  - 写入后需要等待内部写周期，建议先延时 5~10 ms 或做 ACK polling。
  - `WP` 脚需要保持低电平才允许写入。

## 外部 ADC：AD7124-8

- **芯片**：`AD7124-8`
- **接口**：SPI1
- **供电**：ADC 模块电源由 `ADC_PWR_EN / PA0` 使能，关联 `+2.8V_ADC`
- **CS**：`CS#` 接地，ADC 常选中
- **引脚映射**：
  - `PA0`：`ADC_PWR_EN`
  - `PA1`：`SPI1_SCK`
  - `PA6`：`SPI1_MISO` / `DOUT/RDY#`
  - `PA7`：`SPI1_MOSI` / `DIN`
- **CubeMX 配置要点**：
  - Full-Duplex Master
  - Motorola frame
  - 8-bit data size
  - MSB first
  - SPI Mode 3：CPOL High，CPHA 2 Edge
  - NSS Software
  - Prescaler：64，当前约 1.25 Mbit/s
- **开发注意**：
  - AD7124 测试前需要先确保 `PWR_ON = 1` 且 `ADC_PWR_EN = 1`。
  - `DOUT/RDY#` 低电平表示数据准备好。
  - 初期建议先读 ID 寄存器验证 SPI 通信，AD7124-8 的 ID 高半字节应为 `0x1`，常见读数为 `0x12`。
  - `CS#` 接地意味着 SPI1 总线不适合再与其他器件共用。
  - 因 CS 常接地，如 SPI 通信失步，可发送至少 64 个 SCLK 且 DIN 保持高电平来复位 AD7124 串行接口。
  - ADC参考电压2.048V
## 电池电压检测

- **使能信号**：`BAT_ADC_EN`，`PC1`
- **ADC 输入**：`BAT_V_ADC`，`PC0 / ADC1_IN1`
- **控制逻辑**：
  - `BAT_ADC_EN = 1`：打开电池分压采样电路
  - `BAT_ADC_EN = 0`：关闭电池分压采样电路，降低功耗
- **分压电阻**：`R37 = 10kΩ`，`R20 = 10kΩ`
- **分压关系**：`BAT_V_ADC = VBAT / 2`
- **滤波电容**：`C30 = 100nF`
- **CubeMX 配置要点**：
  - ADC1 IN1 Single-ended
  - 12-bit resolution
  - Right alignment
  - Software trigger
  - Sampling time：247.5 cycles
  - ADC clock：32 MHz，ADC prescaler Async DIV4
- **开发注意**：
  - 采样流程建议：拉高 `BAT_ADC_EN`，延时 5~10 ms，多次采样取平均，然后拉低 `BAT_ADC_EN`。
  - 电压换算：`VBAT = ADC_RAW / 4095.0 * VDDA * 2`。
  - 当前硬件 VDDA 约为 `2.8V`，不要按 `3.3V` 直接换算。
  - 当前 `stm32l4xx_hal_conf.h` 里的 `VDD_VALUE` 仍为 `3300U`，若代码中使用该宏计算电压，需要改用实测 VDDA 或在 CubeMX/工程配置中统一确认。
  - 建议正式测量前执行 ADC 校准。

## 按键与指示灯

- **LED**：
  - `PB0`：`LED_GREEN`
  - `PB1`：`LED_YELLOW`
  - `PB2`：`LED_RED`
  - 实测为高电平点亮
  - CubeMX 当前初始输出为 Low，默认熄灭
- **按键/输入**：
  - `PB13`：`ON_OFF`，GPIO Input / No Pull
  - `PB14`：`KEY_PWR`，GPIO Input / No Pull
  - `PB15`：`KEY0`，GPIO Input / Pull-up
- **开发注意**：
  - `ON_OFF`、`KEY_PWR` 是否悬空取决于外部电源/按键电路；如实测抖动或悬空，应在 CubeMX 中配置合适上下拉。
  - `KEY0` 已启用内部上拉，通常按下接地时为低电平有效。

## 串口

- **USART1**：
  - `PA9`：TX
  - `PA10`：RX
  - 当前用于 `printf` 调试输出
- **USART2**：
  - `PA2`：TX
  - `PA3`：RX
- **USART3**：
  - `PC4`：TX
  - `PC5`：RX
- **默认配置**：115200，8N1，无硬件流控

## 定时器与看门狗

- **IWDG**：
  - 已启用
  - Prescaler：16
  - Reload：默认 4095
  - 主循环中需要定期调用 `HAL_IWDG_Refresh()`
- **TIM6**：
  - Prescaler：7999
  - Period：49999
  - 当前已初始化，但暂未明确业务用途
- **TIM7**：
  - Prescaler：7999
  - Period：9999
  - 当前已初始化，但暂未明确业务用途

## 当前软件开发注意事项

- **用户代码区域**：新增业务代码应写在 `USER CODE BEGIN` 与 `USER CODE END` 之间，避免 CubeMX 重新生成时被覆盖。
- **电源初始化顺序**：
  - `PWR_ON` 用于电源保持时，应尽早拉高。
  - AD7124 测试前应拉高 `ADC_PWR_EN` 并延时。
  - SD 卡访问前应拉高 `SD_PWR_EN` 并延时。
- **SD 卡初始化顺序**：如果 `SD_PWR_EN` 默认低电平，则在 `f_mount()` 前必须先打开 SD 电源并延时。
- **FatFs 挂载**：`MX_FATFS_Init()` 只负责链接驱动，真正访问 SD 卡通常从 `f_mount()` 开始。
- **FatFs 时间戳**：当前 `get_fattime()` 返回 0，后续如果需要正确文件时间，需要启用 RTC 或外部时间源。
- **低功耗/未用外设**：TIM6、TIM7、USART2、USART3 如短期不用，可后续在 CubeMX 中关闭或延后初始化，减少功耗和代码复杂度。
