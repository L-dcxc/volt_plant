# 差分设置修改说明

## 修改日期
2026-05-31

## 修改原因
硬件工程师反馈：差分只能允许固定的相邻的两两通道差分，电路上做了限制。
- AIN0 只能和 AIN1 差分
- AIN2 只能和 AIN3 差分
- 以此类推...

## 修改内容

### 1. 通道与AIN固定对应
- **修改前**：用户可以自由选择任意AIN作为通道的正输入
- **修改后**：通道n固定使用AINn（通道0用AIN0，通道1用AIN1，以此类推）
- **UI变化**：正输入下拉框已禁用，显示为灰色

### 2. 差分模式自动配对
- **修改前**：用户可以自由选择任意AIN作为差分的负输入
- **修改后**：根据硬件配对规则自动设置负输入
  - 偶数通道（0,2,4...）差分时：负输入 = AIN(n+1)
  - 奇数通道（1,3,5...）差分时：负输入 = AIN(n-1)
  - 单端模式：负输入 = AVSS(17)
- **UI变化**：负输入下拉框已禁用，显示为灰色

### 3. 差分互斥逻辑
- **修改前**：所有通道可以独立配置，没有互斥关系
- **修改后**：相邻通道差分互斥
  - 通道0设为差分 → 通道1自动禁用（整行灰色）
  - 通道1设为差分 → 通道0自动禁用（整行灰色）
  - 两者都是单端 → 两者都可编辑
- **UI变化**：被禁用的通道整行显示为灰色，不可编辑

## 使用示例

### 场景1：测量6路树干水分传感器（差分模式）
假设需要测量6路0~2mV的树干水分传感器，使用差分模式抗干扰：

1. **通道0**：设为差分
   - 正输入：AIN0（自动固定）
   - 负输入：AIN1（自动设置）
   - 通道1自动禁用（灰色）
   
2. **通道2**：设为差分
   - 正输入：AIN2（自动固定）
   - 负输入：AIN3（自动设置）
   - 通道3自动禁用（灰色）
   
3. **通道4**：设为差分
   - 正输入：AIN4（自动固定）
   - 负输入：AIN5（自动设置）
   - 通道5自动禁用（灰色）

以此类推...

**结果**：6路差分测量占用12个AIN引脚（AIN0-AIN11）

### 场景2：混合使用单端和差分
假设需要：
- 1路温湿度传感器（0~2V，单端）
- 1路光合有效辐射（0~2V，单端）
- 3路土壤含水（0~2V，单端）
- 6路树干水分（0~2mV，差分）

配置方案：
- **通道0-4**：单端模式（温湿度、光合、3路土壤）
  - 使用AIN0-AIN4，负输入自动设为AVSS
  
- **通道5**：单端模式（保持可用）
  
- **通道6,8,10,12,14**：差分模式（6路树干水分）
  - 通道6：AIN6-AIN7（通道7自动禁用）
  - 通道8：AIN8-AIN9（通道9自动禁用）
  - 通道10：AIN10-AIN11（通道11自动禁用）
  - 通道12：AIN12-AIN13（通道13自动禁用）
  - 通道14：AIN14-AIN15（通道15自动禁用）

## 注意事项

1. **作为负输入的通道不需要单独配置**
   - 当通道0设为差分时，AIN1作为负输入已被占用
   - 通道1会自动禁用，无需配置其他参数
   - 增益、滤波等参数在通道0上配置，会作用于整个差分对

2. **从设备读取配置时**
   - 上位机会自动根据模式设置正负输入
   - 自动刷新通道的互斥状态
   - 被禁用的通道会显示为灰色

3. **写入设备时**
   - 正负输入会根据通道号和模式自动计算
   - 无需担心手动配置错误

4. **帮助文档**
   - 点击配置页右上角的"说明书"按钮
   - 查看详细的字段说明和配置示例

## 技术细节

### 修改的文件
- `upper_computer/app/tabs/config_tab.py`

### 新增的方法
- `_on_mode_changed(ch, mode_idx)`: 模式改变时的回调
- `_update_channel_pair_state(changed_ch)`: 更新配对通道状态
- `_set_channel_enabled(ch, enabled)`: 设置通道启用/禁用
- `_refresh_all_channel_states()`: 刷新所有通道状态

### 修改的方法
- `_build_channel_box()`: 固定正负输入，添加模式切换信号
- `_set_channel_widgets(ch, r)`: 读取时自动设置正负输入
- `_build_channel_regs(ch)`: 写入时自动计算正负输入
- `read_from_device()`: 读取后刷新互斥状态

## 测试建议

运行上位机进行手动测试：
```bash
cd upper_computer
python run.py
```

测试要点：
1. ✅ 切换通道0为差分，观察通道1是否变灰
2. ✅ 切换通道1为差分，观察通道0是否变灰
3. ✅ 两者都设为单端，观察是否都可编辑
4. ✅ 检查负输入是否自动正确设置
5. ✅ 测试其他通道对（2-3, 4-5, 6-7, ...）
6. ✅ 验证正输入固定为AINn且不可编辑

## 常见问题

**Q: 为什么正输入和负输入都是灰色的，不能选择？**
A: 这是新的设计。为了符合硬件限制，正负输入已经自动管理，无需手动选择。通道n固定使用AINn，差分模式下负输入根据配对规则自动设置。

**Q: 为什么我设置通道0为差分后，通道1变灰了？**
A: 这是正常的。因为通道0差分时使用了AIN0-AIN1，AIN1已经被占用作为负输入，所以通道1（对应AIN1）不能再单独使用。

**Q: 如果我想用AIN1作为单端测量怎么办？**
A: 确保通道0是单端模式，然后通道1就可以正常使用了。

**Q: 差分模式下，通道1的参数还需要配置吗？**
A: 不需要。当通道0设为差分时，通道1会被禁用。所有参数（增益、滤波等）在通道0上配置即可，会作用于整个差分对（AIN0-AIN1）。

**Q: 我可以让通道0用AIN2作为正输入吗？**
A: 不可以。新的设计中，通道n固定使用AINn。这是为了符合硬件电路的限制。

## 版本信息
- 修改前版本：原始版本（无差分限制）
- 修改后版本：2026-05-31（添加硬件差分限制）
